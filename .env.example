# MongoDB Atlas connection string (or local mongodb URI)
# Note: remember to URL-encode special characters in passwords (e.g. '@' becomes '%40')
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.sqv3q1c.mongodb.net/event-portal?appName=Cluster0

# JSON Web Token Secret
JWT_SECRET=your_jwt_secret_key_here
